name: Fix loading screen

on:
  workflow_dispatch:
  push:
    paths:
      - scripts/fix-loading-screen.py
      - .github/workflows/fix-loading-screen.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/fix-loading-screen.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html mytrips/index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Fix loading bar contrast, status text, and picker overlap" && git push; fi
