name: Scheduled share page
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/scheduled-share-page.py
      - .github/workflows/scheduled-share-page.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/scheduled-share-page.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Show future train schedule and route on shared pages" && git push; fi
