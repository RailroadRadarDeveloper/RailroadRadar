name: MyTrips homepage
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/mytrips-homepage.py
      - .github/workflows/mytrips-homepage.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/mytrips-homepage.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html mytrips/index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Add a MyTrips homepage for signed-out visitors" && git push; fi
