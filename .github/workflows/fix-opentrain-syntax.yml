name: Fix openTrainSchedule syntax
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/fix-opentrain-syntax.py
      - .github/workflows/fix-opentrain-syntax.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/fix-opentrain-syntax.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Fix Uncaught SyntaxError in station popup click" && git push; fi
