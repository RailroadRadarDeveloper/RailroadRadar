name: Trip download 9:16 single logo
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/trip-download-916.py
      - .github/workflows/trip-download-916.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/trip-download-916.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html mytrips/index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Make trip image downloads 9:16 and stamp the logo once" && git push; fi
