name: Fix hidden MyTrips cards
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/fix-mytrips-hidden-cards.py
      - .github/workflows/fix-mytrips-hidden-cards.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/fix-mytrips-hidden-cards.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html mytrips/index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Unhide MyTrips trip cards after grouping upcoming and past" && git push; fi
