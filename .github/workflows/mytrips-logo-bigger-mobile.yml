name: Bigger mobile MyTrips logo
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/mytrips-logo-bigger-mobile.py
      - .github/workflows/mytrips-logo-bigger-mobile.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/mytrips-logo-bigger-mobile.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html mytrips/index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Make mobile MyTrips logo a bit larger" && git push; fi
