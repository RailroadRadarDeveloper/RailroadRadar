name: MyTrips logos link home
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/mytrips-logo-home.py
      - .github/workflows/mytrips-logo-home.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/mytrips-logo-home.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html mytrips/index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Point MyTrips logos to /mytrips/ instead of the live map" && git push; fi
