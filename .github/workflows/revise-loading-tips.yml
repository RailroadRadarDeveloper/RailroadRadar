name: Revise loading tips
on:
  push:
    paths:
      - 'scripts/revise-loading-tips.py'
      - '.github/workflows/revise-loading-tips.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python3 scripts/revise-loading-tips.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then echo "No changes"; exit 0; fi
          git commit -m "Keep favorite loading tips and swap the rest"
          git push
