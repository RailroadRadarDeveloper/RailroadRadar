name: Fix duplicate svcDate
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/fix-svcdate-dup.py
      - .github/workflows/fix-svcdate-dup.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/fix-svcdate-dup.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Remove duplicate svcDate in station popup" && git push; fi
