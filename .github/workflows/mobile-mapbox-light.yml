name: Mobile Mapbox Light default
on:
  push:
    paths:
      - 'scripts/mobile-mapbox-light.py'
      - '.github/workflows/mobile-mapbox-light.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python3 scripts/mobile-mapbox-light.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then echo "No changes"; exit 0; fi
          git commit -m "Default mobile map to regular Mapbox Light"
          git push
