name: Departs in and phrasing
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/departs-in-and.py
      - .github/workflows/departs-in-and.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/departs-in-and.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Say and in Departs in countdown" && git push; fi
