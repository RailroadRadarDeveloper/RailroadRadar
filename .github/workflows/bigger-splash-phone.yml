name: Bigger splash phone
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/bigger-splash-phone.py
      - .github/workflows/bigger-splash-phone.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/bigger-splash-phone.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html mytrips/index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Make MyTrips splash phone mockup larger on desktop" && git push; fi
