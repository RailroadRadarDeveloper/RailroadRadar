name: Use hosted MBTA icon
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/use-hosted-mbta-icon.py
      - .github/workflows/use-hosted-mbta-icon.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/use-hosted-mbta-icon.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Use hosted MBTA icon file in train headings" && git push; fi
