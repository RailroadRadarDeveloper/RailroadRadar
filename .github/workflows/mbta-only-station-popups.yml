name: MBTA-only station popups
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/mbta-only-station-popups.py
      - .github/workflows/mbta-only-station-popups.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/mbta-only-station-popups.py
      - run: |
          git config user.name "RailroadRadar"
          git config user.email "67028963+RailroadRadarDeveloper@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then echo nothing; else git commit -m "Disable Amtrak station popups; keep MBTA only" && git push; fi
